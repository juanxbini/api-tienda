
import Product from '../models/productModel.js';

class ProductController {
  // Métodos para manejar operaciones CRUD para productos
  async getAvailableProductsByCategory(req, res) {
    // Llamar al método correspondiente en el modelo con la categoría proporcionada
    // ...
  }

  async getProductsByCategoryOrderedByPrice(req, res) {
    // Llamar al método correspondiente en el modelo con la categoría y orden proporcionados
    // ...
  }
}

export default ProductController;
